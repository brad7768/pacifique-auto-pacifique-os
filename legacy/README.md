# Legacy Pacifique Auto (préservé)

Ce dossier contient le **site HTML/CSS/JS historique**, déplacé intact lors de la Phase 1.

**Ne pas supprimer.** Sert de :

- référence visuelle / contenu ;
- rollback ;
- source d’extraction d’assets (déjà fait pour le JPEG hero).

Le site de production Phase 1+ est l’application Astro à la racine du dépôt.

Fichiers notables :

- `index.html` / `index2.html` / pages métier
- `styles.css` / `main.js`
- `netlify.toml` (ancien)
- `1BFC44D9-….png` — architecture visuelle (copie aussi dans `public/assets/reference/`)
